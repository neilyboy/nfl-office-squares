# Database Configuration
DATABASE_URL="postgresql://squares:your_secure_password@db:5432/nfl_squares?schema=public"

# PostgreSQL Credentials
POSTGRES_USER=squares
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=nfl_squares

# Application Settings
NODE_ENV=production
PORT=3002
TZ=America/Chicago

# Security Settings
# Set to true if behind HTTPS reverse proxy, false for HTTP
FORCE_SECURE_COOKIES=false

# Admin Configuration (Optional - can be set via /setup page instead)
# Generate these by going through the setup page first, then export them
# ADMIN_PIN_HASH=
# ADMIN_PASSWORD_HASH=

# Development (for local development only, not needed for Docker)
# DATABASE_URL="file:./data/nfl-squares.db"
